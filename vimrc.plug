if &compatible
    set no compatible
end

let g:has_async = v:version >= 800 || has('nvim')

call plug#begin('~/.vim/plugged')

Plug 'NLKNguyen/papercolor-theme'

if g:has_async
    Plug 'w0rp/ale'
endif


call plug#end()
